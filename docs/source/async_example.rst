Async Example
-------------

Here's another app that performs the same operation as show in the
previous example, but uses asyncio code to perform it's actions:

.. include:: ../../examples/sqla_async.py
   :code: python
